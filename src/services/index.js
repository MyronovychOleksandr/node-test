const { CatsService } = require("./catsService");
const { UserService } = require("./userService");
const { AuthService } = require("./AuthService");

module.exports = { CatsService, UserService, AuthService };
